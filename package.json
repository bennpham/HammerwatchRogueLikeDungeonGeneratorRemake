{
  "name": "hammerwatch-roguelike-dungeon-generator",
  "version": "0.3.0",
  "description": "GUI remake of the Hammerwatch Rogue-like Dungeon Generator",
  "main": "out/main/index.js",
  "author": "bennpham",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "electron-vite dev",
    "start": "electron-vite preview",
    "build": "npm run typecheck && electron-vite build",
    "typecheck": "tsc --noEmit -p tsconfig.node.json && tsc --noEmit -p tsconfig.web.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "dist": "npm run build && electron-builder",
    "dist:win": "npm run build && electron-builder --win",
    "dist:mac": "npm run build && electron-builder --mac",
    "dist:linux": "npm run build && electron-builder --linux"
  },
  "build": {
    "appId": "com.bennpham.hammerwatchgen",
    "productName": "Hammerwatch Dungeon Generator",
    "files": [
      "out/**/*",
      "parameters.default.txt"
    ],
    "directories": {
      "output": "release"
    },
    "win": {
      "target": "nsis"
    },
    "mac": {
      "target": "dmg"
    },
    "linux": {
      "target": "AppImage",
      "category": "Game"
    }
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.2.0",
    "electron": "^43.2.0",
    "electron-builder": "^26.15.3",
    "electron-vite": "^5.0.0",
    "playwright-core": "^1.61.1",
    "typescript": "^7.0.2",
    "vite": "^7.3.6",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "allowScripts": {
    "esbuild@0.25.12": true,
    "esbuild@0.28.1": true
  },
  "//overrides": "All transitive, dev-only (electron-builder build chain). Forces packages onto releases that use minimatch@10/glob@13, which depend on brace-expansion@5.0.8 -- the only line carrying the GHSA-mh99-v99m-4gvg unbounded-expansion guard. The 1.x/2.x backports are NOT patched for it. app-builder-lib exact-pins asar/universal, so overrides are required.",
  "overrides": {
    "@electron/asar": "^4.2.1",
    "@electron/universal": "^3.0.6",
    "filelist": "^2.0.2",
    "minimatch": "^10.2.6"
  }
}
